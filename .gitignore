.env
.venv/
venv/
__pycache__/
*.pyc
.streamlit/secrets.toml
data/optuna.db
# NOTE: these ARE committed on purpose (CI artifacts / cloud state — no persistent
# volume on Streamlit Cloud/GitHub Actions, so anything not committed is lost every run):
#   data/latest_signals.json, data/snapshots.csv, data/journal.db,
#   models/champion_genome.json, models/champion.joblib,
#   models/champion_genome.prev.json, models/champion.prev.joblib, universe.json
